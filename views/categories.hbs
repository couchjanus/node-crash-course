<h1>{{ title }}</h1>
<div>
{{#each categories}}
  <p>{{@index}}. {{this}}</p>
{{/each}}
</div>

<h2>Escape</h2>

<ul>
   {{#each escapearr}}
    <li>
      <span>{{@index}}</span>
      <span>unescaped: {{{this}}} vs. </span>
      <span>escaped: {{this}}</span>
    </li>
  {{/each}}
</ul>
